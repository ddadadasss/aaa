<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.kgc.mapper.AccountMapper">
	<select id="getAccount" resultType="account">
		select * from account where account_number = #{account_number} and account_pwd = #{account_pwd}
	</select>
	<update id="updateAccount" parameterType="account">
		update account set account_money = #{account_money} where account_number = #{account_number}
	</update>
</mapper>